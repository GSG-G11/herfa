import request from './axios';
import {
  Works, Request, AllWorks, OnWork, User, OneService, UserInfoCardProps, OneUserType,
} from './types';

export {
  request,
}; export type {
  Works, Request, AllWorks, OnWork, User, OneService, UserInfoCardProps, OneUserType,
};
