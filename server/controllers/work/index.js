const { getWorkPerPage } = require('./workPerPage');
const { deleteWork } = require('./deleteWork');
const { editWork } = require('./editWork');

module.exports = { getWorkPerPage, editWork, deleteWork };
